# Optional: override the Xero OAuth redirect URI.
# Useful when hosting on a fixed domain (Vercel, Netlify, custom VPS).
# Defaults to window.location.href (origin + path, no hash/query string).
# VITE_REDIRECT_URI=https://yourdomain.com/
